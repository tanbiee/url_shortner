services:
  app:
    build: .
    image: url-shortener:local
    ports:
      - "4041:4041"
    environment:
      - MONGO_HOST=mongodb
      - REDIS_HOST=redis
    depends_on:
      - mongodb
      - redis
    restart: unless-stopped

  mongodb:
    image: mongo:latest
    volumes:
      - mongodb_data:/data/db
    restart: unless-stopped

  redis:
    image: redis:latest
    restart: unless-stopped

volumes:
  mongodb_data:
